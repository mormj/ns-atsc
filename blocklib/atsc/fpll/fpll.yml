module: atsc
block: fpll
label: ATSC FPLL
blocktype: sync_block

parameters:
-   id: rate
    label: Rate
    dtype: float 
    settable: false

ports:
-   domain: stream
    id: in
    direction: input
    type: gr_complex

-   domain: stream
    id: out
    direction: output
    type: float

implementations:
-   id: cpu
# -   id: cuda

file_format: 1
